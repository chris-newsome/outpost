# REGION for CloudWatch Logs
AWS_REGION=us-east-1

# Copy deploy/fluent-bit.conf to /etc/fluent-bit/fluent-bit.conf
# IAM: Prefer an instance profile/role with CloudWatch Logs permissions:
# logs:CreateLogGroup, logs:CreateLogStream, logs:PutLogEvents

