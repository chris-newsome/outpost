# Directory for promtail positions file
PROMTAIL_STATE_DIR=/var/lib/promtail

# Copy promtail config to /etc/promtail/promtail.yaml (see deploy/promtail-config.yml)
# Adjust the Loki URL inside that file as needed.

