# Systemd variables used by the Docker-managed frontend service

# The image to run (use a registry reference or a locally-built tag)
IMAGE=outpost-web:prod

# Host port to expose the web on (container listens on 80)
WEB_PORT=8081

